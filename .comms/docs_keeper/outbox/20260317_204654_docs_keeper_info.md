---
from: docs_keeper
to: qa_lead
type: info
priority: medium
---

BUG FOUND: tools/fix.py fix_handle_gt() has the same false positive that T13 fixed in lint.py. The fixer regex matches ANY `var > 0 then` including non-handle vars like `dist`. Running `python -m tools.fix` would incorrectly change Fire_Locator's `dist > 0 then` (a UiWorldToPixel depth check) to `dist ~= 0 then`. Created task for mod_converter to apply same exclusion list from lint. DO NOT run `python -m tools.fix` until this is patched.