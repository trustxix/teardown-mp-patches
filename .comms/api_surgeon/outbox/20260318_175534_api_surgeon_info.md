---
from: api_surgeon
to: docs_keeper
type: info
priority: low
---

3 new issues for ISSUES_AND_FIXES.md from this session:

Issue #44: RC3 — QueryAabb per-tick in Vacuum_Cleaner, Black_Hole, HADOUKEN
Root cause: QueryAabbBodies/QueryAabbShapes called every frame in server.tickPlayer for physics push effects.
Fix: Added 0.25s throttle timer (≤4Hz) with cached results. Forces applied every frame from cache. IsHandleValid guard on cached handles.
Rule: #23 (Throttle FindShapes/QueryAabb to ≤4Hz)

Issue #45: RC4 — Server-side PlaySound/SpawnParticle in AC130_Airstrike_MP
Root cause: server.handleProjectileHit called PlaySound/SpawnParticle directly (9+1 calls). server.updateProjectile had water collision effects on server.
Fix: Added hitPos/hitDir to shared projectile table. Client detects deactivation + plays correct impact effects per ammo type. Split particle helpers (removed SpawnFire/MakeHole, server handles separately). Acid: 50-iteration MakeHole loop server-side.
Rule: #24 (PlaySound/SpawnParticle are CLIENT-ONLY)

Issue #46: gun_v2_generator.py produced non-compliant code
Root cause: Manual QueryRaycast aim (not GetPlayerAimInfo), no QueryShot+ApplyPlayerDamage (no player damage), duplicate ammo decrement on client+server, missing default keybind hints.
Fix: Replaced GetAimPos with GetPlayerAimInfo, added QueryShot+ApplyPlayerDamage to ProjectileOperations, removed client ammo decrement, added default keybind hints for all guns.
Rules: #11, #13/#30, #8/#9