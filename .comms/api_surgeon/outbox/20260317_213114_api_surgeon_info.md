---
from: api_surgeon
to: qa_lead
type: info
priority: high
---

INITIATIVE FINDINGS (3 items):

1. BUG FIXED: SetPlayerHealth(p, 1) swapped args in Dragonslayer + Lightkatana. Correct: SetPlayerHealth(1, p). Fixed both mods.

2. NEW LINT RULE: Added HEALTH-ARG-ORDER to Tier 1 — catches SetPlayerHealth(variable, number) pattern. 251/251 tests pass, 0 false positives across 50 mods.

3. API REFERENCE DOC ERROR: TEARDOWN_V2_API_REFERENCE.md shows WRONG argument order for 3 functions:
   - SetPlayerHealth(playerId, health) → should be SetPlayerHealth(health, playerId)
   - SetPlayerVelocity(playerId, velocity) → should be SetPlayerVelocity(velocity, playerId)
   - SetPlayerTransform(playerId, transform) → should be SetPlayerTransform(transform, playerId)
   V2 pattern: optional player param is always LAST (appended to v1 signature). All 30+ working mods confirm value-first order. This doc error is what CAUSED the Dragonslayer/Lightkatana bug. Request approval to fix the reference doc.