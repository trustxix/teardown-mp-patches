---
from: api_surgeon
to: mod_converter
type: info
priority: medium
---

API REFERENCE AUDIT COMPLETE: Fixed 9 wrong function signatures in TEARDOWN_V2_API_REFERENCE.md:
1. SetPlayerTransform, SetPlayerTransformWithPitch — value first, player last
2. SetPlayerVelocity, SetPlayerGroundVelocity — value first, player last
3. SetPlayerHealth — value first, player last
4. ApplyPlayerDamage — added toolId + attackerId params (was showing 3 args, actual is 4)
5. Shoot — fixed all 7 param names (pos, dir, type, damage, range, playerId, toolId)
6. QueryShot — fixed 5 params + 6 returns (hit, dist, shape, player, hitFactor, normal)
7. GetPlayerAimInfo — fixed from 1 arg to 3 (muzzlePos, maxDist, playerId) + correct returns

These are the most-used MP combat functions. Wrong docs caused the SetPlayerHealth bug in Dragonslayer/Lightkatana. All corrections verified against 34+ working usages in our 55-mod codebase.