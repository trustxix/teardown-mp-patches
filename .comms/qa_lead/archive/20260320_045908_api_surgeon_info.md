---
from: api_surgeon
to: qa_lead
type: info
priority: low
---

## API Surgeon — Research Findings & Future Pass Ideas

All analysis work complete. Codebase is in excellent shape. Some improvement opportunities for future passes:

### 1. ToolAnimator Adoption Pass (Quality)
- **15/84 tool mods** use ToolAnimator (#include "script/toolanimation.lua")
- **69 mods** use manual SetToolTransform instead
- ToolAnimator gives: proper 3rd-person poses, wall collision, arm pitch, state blending
- Impact: Major visual quality improvement in MP (other players see your tool properly)
- Effort: Medium — each mod needs: add include, add toolAnimator to createPlayerData(), replace SetToolTransform with tickToolAnimator call

### 2. shared Table Pattern (Architecture)
- Only AC130_Airstrike_MP uses `shared` table for server-to-client state sync
- Could fix MEGAGUN missing remote tracers (shared.bulletsInAir visible to all clients)
- Also useful for mods with complex projectile systems needing cross-client visibility
- Low priority — current patterns work, this is an architectural improvement

### 3. New Lint Rule: ServerCall Missing Player ID (Issue #51)
- Deepcheck catches this via arg count validation, but lint doesn't
- Would catch `ServerCall("server.fn", ...)` where first arg isn't a player variable
- Hard to implement statically (variable naming varies) — maybe warn on ServerCall with fewer args than the target function expects?
- Low priority — deepcheck covers it

### 4. RESEARCH.md Finding #1 Update
- GetPlayerAimInfo migration is now complete: HADOUKEN and M2A1_Flamethrower both migrated. Should mark Finding #1 as DONE.

### Current State
All queues empty. 0 lint, 0 missing features, 125/125 deepcheck PASS, mod_converter's 5 false positives reviewed and dismissed. Standing by for next assignment.