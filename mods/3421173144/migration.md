# Migration Report: British\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/British\humanoid.lua
+++ patched/British\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/British/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/British/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/British/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/British/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/British/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/British/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/British/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/British/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/British/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/British/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/British/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/British/snd/pain1.ogg", 9.0), LoadSound("MOD/British/snd/pain2.ogg", 9.0), LoadSound("MOD/British/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/British/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: British\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/British\humanoidfriendly.lua
+++ patched/British\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/British/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/British/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/British/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/British/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/British/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/British/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/British/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/British/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/British/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/British/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/British/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/British/snd/pain1.ogg", 9.0), LoadSound("MOD/British/snd/pain2.ogg", 9.0), LoadSound("MOD/British/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/British/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: British\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/British\humanoidhostile.lua
+++ patched/British\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/British/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/British/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/British/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/British/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/British/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/British/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/British/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/British/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/British/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/British/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/British/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/British/snd/pain1.ogg", 9.0), LoadSound("MOD/British/snd/pain2.ogg", 9.0), LoadSound("MOD/British/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/British/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: British2\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/British2\humanoid.lua
+++ patched/British2\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/British2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/British2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/British2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/British2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/British2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/British2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/British2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/British2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/British2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/British2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/British2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/British2/snd/pain1.ogg", 9.0), LoadSound("MOD/British2/snd/pain2.ogg", 9.0), LoadSound("MOD/British2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/British2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: British2\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/British2\humanoidfriendly.lua
+++ patched/British2\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/British/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/British/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/British/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/British/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/British/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/British/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/British/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/British/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/British/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/British/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/British/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/British/snd/pain1.ogg", 9.0), LoadSound("MOD/British/snd/pain2.ogg", 9.0), LoadSound("MOD/British/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/British/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: British2\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/British2\humanoidhostile.lua
+++ patched/British2\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/British/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/British/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/British/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/British/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/British/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/British/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/British/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/British/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/British/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/British/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/British/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/British/snd/pain1.ogg", 9.0), LoadSound("MOD/British/snd/pain2.ogg", 9.0), LoadSound("MOD/British/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/British/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: Cartel\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/Cartel\humanoid.lua
+++ patched/Cartel\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/Cartel/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Cartel/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/Cartel/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/Cartel/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Cartel/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/Cartel/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/Cartel/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/Cartel/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/Cartel/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/Cartel/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/Cartel/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/Cartel/snd/pain1.ogg", 9.0), LoadSound("MOD/Cartel/snd/pain2.ogg", 9.0), LoadSound("MOD/Cartel/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/Cartel/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: Cartel\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/Cartel\humanoidfriendly.lua
+++ patched/Cartel\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/Cartel/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Cartel/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/Cartel/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/Cartel/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Cartel/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/Cartel/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/Cartel/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/Cartel/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/Cartel/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/Cartel/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/Cartel/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/Cartel/snd/pain1.ogg", 9.0), LoadSound("MOD/Cartel/snd/pain2.ogg", 9.0), LoadSound("MOD/Cartel/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/Cartel/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: Cartel\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/Cartel\humanoidhostile.lua
+++ patched/Cartel\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/Cartel/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Cartel/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/Cartel/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/Cartel/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Cartel/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/Cartel/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/Cartel/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/Cartel/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/Cartel/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/Cartel/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/Cartel/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/Cartel/snd/pain1.ogg", 9.0), LoadSound("MOD/Cartel/snd/pain2.ogg", 9.0), LoadSound("MOD/Cartel/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/Cartel/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: Cartel2\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/Cartel2\humanoid.lua
+++ patched/Cartel2\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/Cartel2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Cartel2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/Cartel2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/Cartel2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Cartel2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/Cartel2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/Cartel2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/Cartel2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/Cartel2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/Cartel2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/Cartel2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/Cartel2/snd/pain1.ogg", 9.0), LoadSound("MOD/Cartel2/snd/pain2.ogg", 9.0), LoadSound("MOD/Cartel2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/Cartel2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: Cartel2\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/Cartel2\humanoidfriendly.lua
+++ patched/Cartel2\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/Cartel2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Cartel2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/Cartel2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/Cartel2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Cartel2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/Cartel2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/Cartel2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/Cartel2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/Cartel2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/Cartel2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/Cartel2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/Cartel2/snd/pain1.ogg", 9.0), LoadSound("MOD/Cartel2/snd/pain2.ogg", 9.0), LoadSound("MOD/Cartel2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/Cartel2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: Cartel2\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/Cartel2\humanoidhostile.lua
+++ patched/Cartel2\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/Cartel2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Cartel2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/Cartel2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/Cartel2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Cartel2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/Cartel2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/Cartel2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/Cartel2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/Cartel2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/Cartel2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/Cartel2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/Cartel2/snd/pain1.ogg", 9.0), LoadSound("MOD/Cartel2/snd/pain2.ogg", 9.0), LoadSound("MOD/Cartel2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/Cartel2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: CombineCP\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/CombineCP\humanoid.lua
+++ patched/CombineCP\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/CombineCP/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/CombineCP/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/CombineCP/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/CombineCP/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/CombineCP/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/CombineCP/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/CombineCP/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/CombineCP/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/CombineCP/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/CombineCP/snd/pain1.ogg", 9.0), LoadSound("MOD/CombineCP/snd/pain2.ogg", 9.0), LoadSound("MOD/CombineCP/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/CombineCP/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: CombineCP\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/CombineCP\humanoidfriendly.lua
+++ patched/CombineCP\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/CombineCP/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/CombineCP/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/CombineCP/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/CombineCP/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/CombineCP/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/CombineCP/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/CombineCP/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/CombineCP/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/CombineCP/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/CombineCP/snd/pain1.ogg", 9.0), LoadSound("MOD/CombineCP/snd/pain2.ogg", 9.0), LoadSound("MOD/CombineCP/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/CombineCP/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: CombineCP\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/CombineCP\humanoidhostile.lua
+++ patched/CombineCP\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/CombineCP/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/CombineCP/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/CombineCP/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/CombineCP/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/CombineCP/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/CombineCP/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/CombineCP/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/CombineCP/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/CombineCP/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/CombineCP/snd/pain1.ogg", 9.0), LoadSound("MOD/CombineCP/snd/pain2.ogg", 9.0), LoadSound("MOD/CombineCP/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/CombineCP/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: CombineCP\safe.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/CombineCP\safe.lua
+++ patched/CombineCP\safe.lua
@@ -1,14 +1,7 @@
-#include "script/common.lua"
-
-function init()
-SetTag(handle, "enemy")
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -40,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -111,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -128,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -214,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -228,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -239,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -256,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -271,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -323,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -338,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -406,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -453,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -500,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -530,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -554,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -584,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -628,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -661,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -670,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -693,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -706,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -745,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -810,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -986,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1029,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1056,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1150,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1181,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1193,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1257,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1311,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1320,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1337,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1352,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1360,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1389,439 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/CombineCP/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/CombineCP/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/CombineCP/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/CombineCP/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/CombineCP/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/CombineCP/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/CombineCP/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/CombineCP/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/CombineCP/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/CombineCP/snd/pain1.ogg", 9.0), LoadSound("MOD/CombineCP/snd/pain2.ogg", 9.0), LoadSound("MOD/CombineCP/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/CombineCP/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1838,65 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1927,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1962,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -1991,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2017,4 +1341,440 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if humanoid.deleted then 
+        	return
+        else 
+        	if not IsHandleValid(humanoid.body) then
+        		for i=1, #humanoid.allBodies do
+        			Delete(humanoid.allBodies[i])
+        		end
+        		for i=1, #humanoid.allJoints do
+        			Delete(humanoid.allJoints[i])
+        		end
+        		humanoid.deleted = true
+        	end
+        end
+        if humanoid.activateTrigger ~= 0 then 
+        	if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        		RemoveTag(humanoid.body, "inactive")
+        		humanoid.activateTrigger = 0
+        	end
+        end
+        if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        	humanoidCollide(true)
+        else
+        	humanoidCollide(false)
+        end
+        if HasTag(humanoid.body, "inactive") then
+        	humanoid.inactive = true
+        	return
+        else
+        	if humanoid.inactive then
+        		humanoid.inactive = false
+        		--Reset robot pose
+        		local sleep = HasTag(humanoid.body, "sleeping")
+        		for i=1, #humanoid.allBodies do
+        			SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        			SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        			SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        			if sleep then
+        				--If robot is sleeping make sure to not wake it up
+        				SetBodyActive(humanoid.allBodies[i], false)
+        			end
+        		end
+        	end
+        end
+        if HasTag(humanoid.body, "sleeping") then
+        	if IsBodyActive(humanoid.body) then
+        		wakeUp = true
+        	end
+        	local vol, pos = GetLastSound()
+        	if vol > 0.2 then
+        		if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        			wakeUp = true
+        		end
+        	end	
+        	if wakeUp then
+        		RemoveTag(humanoid.body, "sleeping")
+        	end
+        	return
+        end
+        humanoidUpdate()
+        if not humanoid.enabled then
+        	return
+        end
+        if humanoid.health <= 0.0 then
+        	for i = 1, #humanoid.allShapes do
+        		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	end
+        	SetTag(humanoid.body, "disabled")
+        	humanoid.enabled = false
+        	playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        end
+        -- if IsPointInWater(humanoid.bodyCenter) then
+        -- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        -- 	for i=1, #humanoid.allShapes do
+        -- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        -- 	end
+        -- 	SetTag(humanoid.body, "disabled")
+        -- 	humanoid.enabled = false
+        -- end
+        humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        if humanoid.stunned ~= 0 then
+        	head.seenTimer = 0
+        	weaponsReset()
+        	return
+        end
+        hoverUpdate(dt)
+        headUpdate(dt)
+        aimsUpdate(dt)
+        weaponsUpdate(dt)
+        animatorUpdate(dt)
+        hearingUpdate(dt)
+        stackUpdate(dt)
+        humanoid.speedScale = 1
+        humanoid.speed = 0
+        local state = stackTop()
+        if state.id == "none" then
+        	if config.patrol then
+        		stackPush("patrol")
+        	else
+        		stackPush("roam")
+        	end
+        end
+        if state.id == "roam" then
+        	if not state.nextAction then
+        		state.nextAction = "move"
+        	elseif state.nextAction == "move" then
+        		local randomPos
+        		if humanoid.roamTrigger ~= 0 then
+        			randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        			randomPos = truncateToGround(randomPos)
+        		else
+        			local rndAng = rnd(0, 2*math.pi)
+        			randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        		end
+        		local s = stackPush("navigate")
+        		s.timeout = 1
+        		s.pos = randomPos
+        		state.nextAction = "search"
+        	elseif state.nextAction == "search" then
+        		stackPush("search")
+        		state.nextAction = "move"
+        	end
+        end
+        if state.id == "patrol" then
+        	if not state.nextAction then
+        		state.index = getClosestPatrolIndex()
+        		state.nextAction = "move"
+        	elseif state.nextAction == "move" then
+        		markPatrolLocationAsActive(state.index)
+        		local nav = stackPush("navigate")
+        		nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        		state.nextAction = "search"
+        	elseif state.nextAction == "search" then
+        		stackPush("search")
+        		state.index = getNextPatrolIndex(state.index)
+        		state.nextAction = "move"
+        	end
+        end
+        if state.id == "search" then
+        	if state.activeTime > 2.5 then
+        		if not state.turn then
+        			humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        			state.turn = true
+        		end
+        		if state.activeTime > 6.0 then
+        			stackPop()
+        		end
+        	end
+        	if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        		head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        	else
+        		head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        	end
+        end
+        if state.id == "investigate" then
+        	if not state.nextAction then
+        		local pos = state.pos
+        		humanoidTurnTowards(state.pos)
+        		headTurnTowards(state.pos)
+        		local nav = stackPush("navigate")
+        		nav.pos = state.pos
+        		nav.timeout = 5.0
+        		state.nextAction = "search"
+        	elseif state.nextAction == "search" then
+        		stackPush("search")
+        		state.nextAction = "done"
+        	elseif state.nextAction == "done" then
+        		playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        		stackPop()
+        	end	
+        end
+        if state.id == "move" then
+        	humanoidTurnTowards(state.pos)
+        	humanoid.speed = config.speed
+        	head.dir = VecCopy(humanoid.dir)
+        	local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        	if d < 2 then
+        		humanoid.speed = 0
+        		stackPop()
+        	else
+        		if humanoid.blocked > 0.5 then
+        			stackPush("unblock")
+        		end
+        	end
+        end
+        if state.id == "unblock" then
+        	if not state.dir then
+        		if math.random(0, 10) < 5 then
+        			state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        		else
+        			state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        		end
+        		state.dir = VecNormalize(state.dir)
+        	else
+        		humanoid.dir = state.dir
+        		humanoid.speed = -math.min(config.speed, 2.0)
+        		if state.activeTime > 1 then
+        			stackPop()
+        		end
+        	end
+        end
+        --Hunt player
+        if state.id == "hunt" then
+        	if not state.init then
+        		navigationClear()
+        		state.init = true
+        		state.headAngle = 0
+        		state.headAngleTimer = 0
+        	end
+        	if humanoid.distToPlayer < 4.0 then
+        		humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		head.dir = VecCopy(humanoid.dirToPlayer)
+        		humanoid.speed = 0
+        		navigationClear()
+        	else
+        		navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+        	end
+        	if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        		--Turn towards player if not moving
+        		humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        	end
+        	if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        		if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        			stackClear()
+        			local s = stackPush("investigate")
+        			s.pos = VecCopy(head.lastSeenPos)		
+        		else
+        			stackClear()
+        			stackPush("huntlost")
+        		end
+        	end
+        end
+        if state.id == "huntlost" then
+        	if not state.timer then
+        		state.timer = 6
+        		state.turnTimer = 1
+        	end
+        	state.timer = state.timer - dt
+        	head.dir = VecCopy(humanoid.dir)
+        	if state.timer < 0 then
+        		playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        		stackPop()
+        	else
+        		state.turnTimer = state.turnTimer - dt
+        		if state.turnTimer < 0 then
+        			humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        			state.turnTimer = rnd(0.5, 1.5)
+        		end
+        	end
+        end
+        --Avoid player
+        if state.id == "avoid" then
+        	if not state.init then
+        		navigationClear()
+        		state.init = true
+        		state.headAngle = 0
+        		state.headAngleTimer = 0
+        	end
+
+        	local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        	local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        	navigationSetTarget(avoidTarget, 1.0)
+        	humanoid.speedScale = config.huntSpeedScale
+        	navigationUpdate(dt)
+        	if head.canSeePlayer then
+        		head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        		state.headAngle = 0
+        		state.headAngleTimer = 0
+        	else
+        		state.headAngleTimer = state.headAngleTimer + dt
+        		if state.headAngleTimer > 1.0 then
+        			if state.headAngle > 0.0 then
+        				state.headAngle = rnd(-1.0, -0.5)
+        			elseif state.headAngle < 0 then
+        				state.headAngle = rnd(0.5, 1.0)
+        			else
+        				state.headAngle = rnd(-1.0, 1.0)
+        			end
+        			state.headAngleTimer = 0
+        		end
+        		head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        	end
+
+        	if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        		stackClear()
+        	end
+        end
+        --Get up player
+        if state.id == "getup" then
+        	if not state.time then 
+        		state.time = 0 
+        	end
+        	state.time = state.time + dt
+        	hover.timeSinceContact = 0
+        	if state.time > 2.0 then
+        		stackPop()
+        	else
+        		hoverGetUp()
+        	end
+        end
+        if state.id == "navigate" then
+        	if not state.initialized then
+        		if not state.timeout then state.timeout = 30 end
+        		navigationClear()
+        		navigationSetTarget(state.pos, state.timeout)
+        		state.initialized = true
+        	else
+        		head.dir = VecCopy(humanoid.dir)
+        		navigationUpdate(dt)
+        		if navigation.state == "done" or navigation.state == "fail" then
+        			stackPop()
+        		end
+        	end
+        end
+
+        --React to sound
+        if not stackHas("hunt") then
+        	if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        		stackClear()
+        		playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        		local s = stackPush("investigate")
+        		s.pos = hearing.lastSoundPos	
+        		hearingConsumeSound()
+        	end
+        end
+
+        --Seen player
+        if config.huntPlayer and not stackHas("hunt") then
+        	if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        		stackClear()
+        		playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        		stackPush("hunt")
+        	end
+        end
+
+        --Seen player
+        if config.avoidPlayer and not stackHas("avoid") then
+        	if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        		stackClear()
+        		stackPush("avoid")
+        	end
+        end
+
+        --Get up
+        if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        	stackPush("getup")
+        end
+
+        if IsShapeBroken(GetLightShape(head.eye)) then
+        	config.hasVision = false
+        	config.canSeePlayer = false
+        end
+    end
+end
+

```

---

# Migration Report: CombineCP2\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/CombineCP2\humanoid.lua
+++ patched/CombineCP2\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/CombineCP2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/CombineCP2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/CombineCP2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/CombineCP2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/CombineCP2/snd/pain1.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/pain2.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/CombineCP2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: CombineCP2\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/CombineCP2\humanoidfriendly.lua
+++ patched/CombineCP2\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/CombineCP2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/CombineCP2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/CombineCP2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/CombineCP2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/CombineCP2/snd/pain1.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/pain2.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/CombineCP2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: CombineCP2\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/CombineCP2\humanoidhostile.lua
+++ patched/CombineCP2\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/CombineCP2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/CombineCP2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/CombineCP2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/CombineCP2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/CombineCP2/snd/pain1.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/pain2.ogg", 9.0), LoadSound("MOD/CombineCP2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/CombineCP2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: CombineCP3\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/CombineCP3\humanoid.lua
+++ patched/CombineCP3\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/CombineCP3/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/CombineCP3/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/CombineCP3/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/CombineCP3/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/CombineCP3/snd/pain1.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/pain2.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/CombineCP3/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: CombineCP3\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/CombineCP3\humanoidfriendly.lua
+++ patched/CombineCP3\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/CombineCP3/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/CombineCP3/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/CombineCP3/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/CombineCP3/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/CombineCP3/snd/pain1.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/pain2.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/CombineCP3/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: CombineCP3\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/CombineCP3\humanoidhostile.lua
+++ patched/CombineCP3\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/CombineCP3/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/CombineCP3/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/CombineCP3/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/CombineCP3/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/CombineCP3/snd/pain1.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/pain2.ogg", 9.0), LoadSound("MOD/CombineCP3/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/CombineCP3/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: CombineCP4\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/CombineCP4\humanoid.lua
+++ patched/CombineCP4\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/CombineCP4/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/CombineCP4/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/CombineCP4/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/CombineCP4/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/CombineCP4/snd/pain1.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/pain2.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/CombineCP4/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: CombineCP4\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/CombineCP4\humanoidfriendly.lua
+++ patched/CombineCP4\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/CombineCP4/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/CombineCP4/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/CombineCP4/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/CombineCP4/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/CombineCP4/snd/pain1.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/pain2.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/CombineCP4/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: CombineCP4\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/CombineCP4\humanoidhostile.lua
+++ patched/CombineCP4\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/CombineCP4/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/CombineCP4/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/CombineCP4/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/CombineCP4/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/CombineCP4/snd/pain1.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/pain2.ogg", 9.0), LoadSound("MOD/CombineCP4/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/CombineCP4/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: French\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/French\humanoid.lua
+++ patched/French\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/French/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/French/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/French/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/French/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/French/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/French/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/French/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/French/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/French/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/French/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/French/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/French/snd/pain1.ogg", 9.0), LoadSound("MOD/French/snd/pain2.ogg", 9.0), LoadSound("MOD/French/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/French/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: French\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/French\humanoidfriendly.lua
+++ patched/French\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/French/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/French/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/French/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/French/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/French/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/French/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/French/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/French/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/French/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/French/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/French/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/French/snd/pain1.ogg", 9.0), LoadSound("MOD/French/snd/pain2.ogg", 9.0), LoadSound("MOD/French/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/French/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: French\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/French\humanoidhostile.lua
+++ patched/French\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/French/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/French/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/French/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/French/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/French/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/French/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/French/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/French/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/French/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/French/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/French/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/French/snd/pain1.ogg", 9.0), LoadSound("MOD/French/snd/pain2.ogg", 9.0), LoadSound("MOD/French/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/French/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: French2\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/French2\humanoid.lua
+++ patched/French2\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/French2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/French2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/French2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/French2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/French2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/French2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/French2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/French2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/French2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/French2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/French2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/French2/snd/pain1.ogg", 9.0), LoadSound("MOD/French2/snd/pain2.ogg", 9.0), LoadSound("MOD/French2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/French2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: French2\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/French2\humanoidfriendly.lua
+++ patched/French2\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/French2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/French2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/French2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/French2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/French2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/French2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/French2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/French2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/French2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/French2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/French2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/French2/snd/pain1.ogg", 9.0), LoadSound("MOD/French2/snd/pain2.ogg", 9.0), LoadSound("MOD/French2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/French2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: French2\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/French2\humanoidhostile.lua
+++ patched/French2\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/French2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/French2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/French2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/French2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/French2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/French2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/French2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/French2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/French2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/French2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/French2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/French2/snd/pain1.ogg", 9.0), LoadSound("MOD/French2/snd/pain2.ogg", 9.0), LoadSound("MOD/French2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/French2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: German\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/German\humanoid.lua
+++ patched/German\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/German/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/German/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/German/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/German/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/German/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/German/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/German/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/German/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/German/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/German/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/German/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/German/snd/pain1.ogg", 9.0), LoadSound("MOD/German/snd/pain2.ogg", 9.0), LoadSound("MOD/German/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/German/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: German\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/German\humanoidfriendly.lua
+++ patched/German\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/German/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/German/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/German/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/German/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/German/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/German/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/German/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/German/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/German/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/German/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/German/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/German/snd/pain1.ogg", 9.0), LoadSound("MOD/German/snd/pain2.ogg", 9.0), LoadSound("MOD/German/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/German/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: German\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/German\humanoidhostile.lua
+++ patched/German\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/German/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/German/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/German/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/German/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/German/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/German/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/German/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/German/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/German/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/German/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/German/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/German/snd/pain1.ogg", 9.0), LoadSound("MOD/German/snd/pain2.ogg", 9.0), LoadSound("MOD/German/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/German/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: German2\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/German2\humanoid.lua
+++ patched/German2\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/German2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/German2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/German2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/German2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/German2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/German2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/German2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/German2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/German2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/German2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/German2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/German2/snd/pain1.ogg", 9.0), LoadSound("MOD/German2/snd/pain2.ogg", 9.0), LoadSound("MOD/German2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/German2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: German2\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/German2\humanoidfriendly.lua
+++ patched/German2\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/German2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/German2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/German2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/German2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/German2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/German2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/German2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/German2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/German2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/German2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/German2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/German2/snd/pain1.ogg", 9.0), LoadSound("MOD/German2/snd/pain2.ogg", 9.0), LoadSound("MOD/German2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/German2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: German2\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/German2\humanoidhostile.lua
+++ patched/German2\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/German2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/German2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/German2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/German2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/German2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/German2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/German2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/German2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/German2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/German2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/German2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/German2/snd/pain1.ogg", 9.0), LoadSound("MOD/German2/snd/pain2.ogg", 9.0), LoadSound("MOD/German2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/German2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: Insurgent2\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/Insurgent2\humanoid.lua
+++ patched/Insurgent2\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/Insurgent2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/Insurgent2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/Insurgent2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/Insurgent2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/Insurgent2/snd/pain1.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/pain2.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/Insurgent2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: Insurgent2\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/Insurgent2\humanoidfriendly.lua
+++ patched/Insurgent2\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/Insurgent2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/Insurgent2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/Insurgent2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/Insurgent2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/Insurgent2/snd/pain1.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/pain2.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/Insurgent2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: Insurgent2\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/Insurgent2\humanoidhostile.lua
+++ patched/Insurgent2\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/Insurgent2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/Insurgent2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/Insurgent2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/Insurgent2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/Insurgent2/snd/pain1.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/pain2.ogg", 9.0), LoadSound("MOD/Insurgent2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/Insurgent2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: PMC2\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/PMC2\humanoid.lua
+++ patched/PMC2\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/PMC2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/PMC2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/PMC2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/PMC2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/PMC2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/PMC2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/PMC2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/PMC2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/PMC2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/PMC2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/PMC2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/PMC2/snd/pain1.ogg", 9.0), LoadSound("MOD/PMC2/snd/pain2.ogg", 9.0), LoadSound("MOD/PMC2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/PMC2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: PMC2\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/PMC2\humanoidfriendly.lua
+++ patched/PMC2\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/PMC2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/PMC2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/PMC2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/PMC2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/PMC2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/PMC2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/PMC2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/PMC2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/PMC2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/PMC2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/PMC2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/PMC2/snd/pain1.ogg", 9.0), LoadSound("MOD/PMC2/snd/pain2.ogg", 9.0), LoadSound("MOD/PMC2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/PMC2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: PMC2\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/PMC2\humanoidhostile.lua
+++ patched/PMC2\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/PMC2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/PMC2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/PMC2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/PMC2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/PMC2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/PMC2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/PMC2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/PMC2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/PMC2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/PMC2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/PMC2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/PMC2/snd/pain1.ogg", 9.0), LoadSound("MOD/PMC2/snd/pain2.ogg", 9.0), LoadSound("MOD/PMC2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/PMC2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: Russian2\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/Russian2\humanoid.lua
+++ patched/Russian2\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/Russian2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Russian2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/Russian2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/Russian2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Russian2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/Russian2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/Russian2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/Russian2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/Russian2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/Russian2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/Russian2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/Russian2/snd/pain1.ogg", 9.0), LoadSound("MOD/Russian2/snd/pain2.ogg", 9.0), LoadSound("MOD/Russian2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/Russian2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: Russian2\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/Russian2\humanoidfriendly.lua
+++ patched/Russian2\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/Russian2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Russian2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/Russian2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/Russian2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Russian2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/Russian2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/Russian2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/Russian2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/Russian2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/Russian2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/Russian2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/Russian2/snd/pain1.ogg", 9.0), LoadSound("MOD/Russian2/snd/pain2.ogg", 9.0), LoadSound("MOD/Russian2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/Russian2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: Russian2\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/Russian2\humanoidhostile.lua
+++ patched/Russian2\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/Russian2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Russian2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/Russian2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/Russian2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/Russian2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/Russian2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/Russian2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/Russian2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/Russian2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/Russian2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/Russian2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/Russian2/snd/pain1.ogg", 9.0), LoadSound("MOD/Russian2/snd/pain2.ogg", 9.0), LoadSound("MOD/Russian2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/Russian2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: USA2\humanoid.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/USA2\humanoid.lua
+++ patched/USA2\humanoid.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/USA2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/USA2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/USA2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/USA2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/USA2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/USA2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/USA2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/USA2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/USA2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/USA2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/USA2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/USA2/snd/pain1.ogg", 9.0), LoadSound("MOD/USA2/snd/pain2.ogg", 9.0), LoadSound("MOD/USA2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/USA2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,93 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-	if InputPressed("x") then
-		_G.friendlyMode = not _G.friendlyMode
-		
-		if _G.friendlyMode == true then
-		DebugPrint("Switching to friendly mode (may takes a few seconds)...")
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	for i=1, #weapons do
-		local w = weapons[i]
-		w.state = "idle"
-		w.fireCount = 0
-		w.chargeTimer = 0
-		w.fireTimer = 0
-	end
-		
-		end
-		if _G.friendlyMode == false then
-		DebugPrint("Switched to hostile mode")
-		configInit()
-		end
-		
-	end
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1968,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -2003,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2032,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2058,4 +1341,476 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+
+function client.tick(dt)
+    local playerId = GetLocalPlayer()
+    if InputPressed("x") then
+    	_G.friendlyMode = not _G.friendlyMode
+
+    	if _G.friendlyMode == true then
+    	DebugPrint("Switching to friendly mode (may takes a few seconds)...")
+    	config.canSeePlayer = false
+    	config.canHearPlayer = false
+    	config.huntPlayer = false
+    	config.aggressive = false
+
+    		-- FORCE STOP FIRING
+    for i=1, #weapons do
+    	local w = weapons[i]
+    	w.state = "idle"
+    	w.fireCount = 0
+    	w.chargeTimer = 0
+    	w.fireTimer = 0
+    end
+
+    	end
+    	if _G.friendlyMode == false then
+    	DebugPrint("Switched to hostile mode")
+    	configInit()
+    	end
+
+    end
+end
+

```

---

# Migration Report: USA2\humanoidfriendly.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/USA2\humanoidfriendly.lua
+++ patched/USA2\humanoidfriendly.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = true
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/USA2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/USA2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/USA2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/USA2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/USA2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/USA2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/USA2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/USA2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/USA2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/USA2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/USA2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/USA2/snd/pain1.ogg", 9.0), LoadSound("MOD/USA2/snd/pain2.ogg", 9.0), LoadSound("MOD/USA2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/USA2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```

---

# Migration Report: USA2\humanoidhostile.lua

## Changes Applied
- Added `#version 2` header
- Split callbacks into `server.*` / `client.*` domains
- Applied automatic fixups (handle checks, deprecated functions, playerId)

## Diff

```diff
--- original/USA2\humanoidhostile.lua
+++ patched/USA2\humanoidhostile.lua
@@ -1,18 +1,7 @@
-#include "script/common.lua"
-_G.friendlyMode = false
-
-
-function init()
-SetTag(handle, "enemy")
-
-
-end
-
-
+#version 2
 function VecDist(a, b)
 	return VecLength(VecSub(a, b))
 end
-
 
 function getTagParameter(entity, name, default)
 	local v = tonumber(GetTagValue(entity, name))
@@ -44,36 +33,6 @@
 		return default, default
 	end
 end
-
-pType = GetStringParam("type", "")
-pSpeed = GetFloatParam("speed", 13)
-pTurnSpeed = GetFloatParam("turnspeed", pSpeed)
-
-config = {}
-config.hasVision = false
-config.viewDistance = 35
-config.viewFov = 200
-config.canHearPlayer = false
-config.canSeePlayer = false
-config.patrol = false
-config.sensorDist = 5.0
-config.speed = pSpeed
-config.turnSpeed = pTurnSpeed
-config.huntPlayer = false
-config.huntSpeedScale = 3
-config.avoidPlayer = false
-config.triggerAlarmWhenSeen = false
-config.visibilityTimer = 0.1 --Time player must be seen to be identified as enemy (ideal condition)
-config.lostVisibilityTimer = 8.0 --Time player is seen after losing visibility
-config.outline = 0
-config.aimTime = 0.0
-config.maxSoundDist = 170.0
-config.aggressive = false
-config.stepSound = "m"
-config.practice = false
-config.maxHealth = 100.0
-
-PATH_NODE_TOLERANCE = 0.8
 
 function configInit()
 	local eye = FindLight("eye")
@@ -115,8 +74,6 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function rndVec(length)
 	local v = VecNormalize(Vec(math.random(-100,100), math.random(-100,100), math.random(-100,100)))
 	return VecScale(v, length)	
@@ -132,49 +89,6 @@
 		QueryRejectBody(bodies[i])
 	end
 end
-
------------------------------------------------------------------------
-
-
-humanoid = {}
-humanoid.body = 0
-humanoid.allBodies = {}
-humanoid.allShapes = {}
-humanoid.allJoints = {}
-humanoid.transform = Transform()
-humanoid.dir = Vec(0, 0, -1)
-humanoid.axes = {}
-humanoid.bodyCenter = Vec()
-humanoid.navigationCenter = Vec()
-humanoid.massCenter = Vec()
-humanoid.speed = 0
-humanoid.speedScale = 1
-humanoid.blocked = 0
-humanoid.mass = 0
-humanoid.initialBodyTransforms = {}
-humanoid.enabled = true
-humanoid.deleted = false
-humanoid.breakAll = false
-humanoid.breakAllTimer = 0
-humanoid.distToPlayer = 100
-humanoid.dirToPlayer = 0
-humanoid.roamTrigger = 0
-humanoid.limitTrigger = 0
-humanoid.investigateTrigger = 0
-humanoid.activateTrigger = 0
-humanoid.stunned = 0
-humanoid.outlineAlpha = 0
-humanoid.canSensePlayer = false
-humanoid.playerPos = Vec()
-humanoid.health = 100.0
-humanoid.headDamageScale = 3.0
-humanoid.torsoDamageScale = 1.4
-humanoid.torso = 0
-humanoid.head = 0
-humanoid.rightHand = 0
-humanoid.leftHand = 0
-humanoid.rightFoot = 0
-humanoid.leftFoot = 0
 
 function humanoidSetAxes()
 	humanoid.transform = GetBodyTransform(humanoid.body)
@@ -218,13 +132,11 @@
 	humanoid.dir = VecNormalize(VecSub(pos, humanoid.transform.pos))
 end
 
-
 function humanoidSetDirAngle(angle)
 	humanoid.dir[1] = math.cos(angle)
 	humanoid.dir[3] = math.sin(angle)
 end
 
-
 function humanoidGetDirAngle()
 	return math.atan2(humanoid.dir[3], humanoid.dir[1])
 end
@@ -232,7 +144,7 @@
 function humanoidUpdate()
 	humanoidSetAxes()
 
-	humanoid.playerPos = GetPlayerCameraTransform().pos
+	humanoid.playerPos = GetPlayerCameraTransform(playerId).pos
 
 	local vel = GetBodyVelocity(humanoid.body)
 	local fwdSpeed = VecDot(vel, humanoid.dir)
@@ -243,7 +155,7 @@
 	humanoid.blocked = humanoid.blocked * 0.95 + blocked * 0.05
 
 	-- --Always blocked if fall is detected
-	-- if sensor.detectFall > 0 then
+	-- if sensor.detectFall ~= 0 then
 	-- 	humanoid.blocked = 1.0
 	-- end
 	
@@ -260,7 +172,7 @@
 	humanoid.massCenter = TransformToParentPoint(humanoid.transform, VecScale(humanoid.massCenter, 1 / humanoid.mass))
 	
 	--Distance and direction to player
-	local pp = VecAdd(GetPlayerTransform().pos, Vec(0, 1, 0))
+	local pp = VecAdd(GetPlayerTransform(playerId).pos, Vec(0, 1, 0))
 	local d = VecSub(pp, humanoid.bodyCenter)
 	humanoid.distToPlayer = VecLength(d)
 	humanoid.dirToPlayer = VecScale(d, 1.0/humanoid.distToPlayer)
@@ -275,22 +187,12 @@
 	end
 end
 
------------------------------------------------------------------------
-
-hover = {}
-hover.hitBody = 0
-hover.contact = 0.0
-hover.currentDist = 0.8
-hover.distTarget = 0.8
-hover.distPadding = 0.3
-hover.timeSinceContact = 0.0
-
 function hoverInit()
 
 end
 
 function hoverFloat()
-	if hover.contact > 0 then
+	if hover.contact ~= 0 then
 		local d = clamp(hover.distTarget - hover.currentDist, -0.2, 0.2)
 		local v = d * 10
 		local f = math.max(0, d * humanoid.mass * 5) + humanoid.mass * 2
@@ -327,7 +229,6 @@
 	ConstrainAngularVelocity(humanoid.body, hover.hitBody, humanoid.axes[2], angVel, -f , f)
 end
 
-
 function hoverMove()
 	local desiredSpeed = humanoid.speed * humanoid.speedScale
 	local fwd = VecScale(humanoid.axes[3], -1)
@@ -342,7 +243,6 @@
 	ConstrainVelocity(humanoid.body, hover.hitBody, humanoid.bodyCenter, humanoid.axes[1], 0, -f , f)
 end
 
-BALANCE_RADIUS = 0.4
 function hoverUpdate()
 	local dir = VecScale(humanoid.axes[2], -1)
 
@@ -410,30 +310,12 @@
 	hoverMove()
 end
 
-------------------------------------------------------------------------
-
-head = {}
-head.body = 0
-head.eye = 0
-head.dir = Vec(0,0,-1)
-head.lookOffset = 0
-head.lookOffsetTimer = 0
-head.canSeePlayer = true
-head.lastSeenPos = Vec(0,0,0)
-head.timeSinceLastSeen = 999
-head.seenTimer = 0
-head.alarmTimer = 0
-head.alarmTime = 2.0
-head.aim = 0	-- 1.0 = perfect aim, 0.0 = will always miss player. This increases when robot sees player based on config.aimTime
-
-
 function headInit()
 	head.body = FindBody("head")
 	head.eye = FindLight("eye")
 	head.joint = FindJoint("head")
 	head.alarmTime = getTagParameter(head.eye, "alarm", 2.0)
 end
-
 
 function headTurnTowards(pos)
 	head.dir = VecNormalize(VecSub(pos, GetBodyTransform(head.body).pos))
@@ -457,7 +339,7 @@
 			local limit = math.cos(config.viewFov * 0.5 * math.pi / 180)
 			if VecDot(toPlayer, fwd) > limit then --In view frustum
 				rejectAllBodies(humanoid.allBodies)
-				QueryRejectVehicle(GetPlayerVehicle())
+				QueryRejectVehicle(GetPlayerVehicle(playerId))
 				if not QueryRaycast(et.pos, toPlayer, distToPlayer, 0, true) then --Not blocked
 					playerVisible = true
 				end
@@ -504,8 +386,8 @@
 				head.alarmTimer = head.alarmTimer + dt
 				PlayLoop(chargeLoop, humanoid.transform.pos)
 				if head.alarmTimer > head.alarmTime and playerVisible then
-					SetString("hud.notification", "Detected by robot. Alarm triggered.")
-					SetBool("level.alarm", true)
+					SetString("hud.notification", "Detected by robot. Alarm triggered.", true)
+					SetBool("level.alarm", true, true)
 				end
 			else
 				head.alarmTimer = math.max(0.0, head.alarmTimer - dt)
@@ -534,21 +416,17 @@
 	-- if ang < mi+1 and angVel < 0 then
 	-- 	angVel = 0
 	-- end
-	-- if ang > ma-1 and angVel > 0 then
+	-- if ang > ma-1 and angVel ~= 0 then
 	-- 	angVel = 0
 	-- end
 
 	ConstrainAngularVelocity(head.body, humanoid.body, humanoid.axes[2], angVel, -f , f)
 
 	local vol = clamp(math.abs(angVel)*0.3, 0.0, 1.0)
-	if vol > 0 then
+	if vol ~= 0 then
 		--PlayLoop(headLoop, humanoid.transform.pos, vol)
 	end
 end
-
-------------------------------------------------------------------------
-
-aims = {}
 
 function aimsInit()
 	local bodies = FindBodies("aim")
@@ -558,7 +436,6 @@
 		aims[i] = aim
 	end
 end
-
 
 function aimsUpdate(dt)
 	local state = stackTop()
@@ -588,10 +465,6 @@
 		ConstrainVelocity(humanoid.body, aim.body, GetBodyTransform(humanoid.body).pos, VecNormalize(GetBodyVelocity(aim.body)), 0, 1)
 	end
 end
-
-------------------------------------------------------------------------
-
-weapons = {}
 
 function weaponsInit()
 	local locs = FindLocations("weapon")
@@ -632,13 +505,11 @@
 	end
 end
 
-
 function getPerpendicular(dir)
 	local perp = VecNormalize(Vec(rnd(-1, 1), rnd(-1, 1), rnd(-1, 1)))
 	perp = VecNormalize(VecSub(perp, VecScale(dir, VecDot(dir, perp))))
 	return perp
 end
-
 
 function weaponFire(weapon, pos, dir)
 	local perp = getPerpendicular(dir)
@@ -665,7 +536,6 @@
 	end
 end
 
-
 function weaponsReset()
 	for i=1, #weapons do
 		weapons[i].state = "idle"
@@ -674,9 +544,8 @@
 	end
 end
 
-
 function weaponEmitFire(weapon, t, amount)
-	if humanoid.stunned > 0 then
+	if humanoid.stunned ~= 0 then
 		return
 	end
 	local p = TransformToParentPoint(t, Vec(0, 0, -0.1))
@@ -697,7 +566,7 @@
 		if not spawnFireTimer then
 			spawnFireTimer = 0
 		end
-		if spawnFireTimer > 0 then
+		if spawnFireTimer ~= 0 then
 			spawnFireTimer = math.max(spawnFireTimer-0.01667, 0)
 		else
 			rejectAllBodies(humanoid.allBodies)
@@ -710,22 +579,21 @@
 		end
 		
 		--Hurt player
-		local toPlayer = VecSub(GetPlayerCameraTransform().pos, t.pos)
+		local toPlayer = VecSub(GetPlayerCameraTransform(playerId).pos, t.pos)
 		local distToPlayer = VecLength(toPlayer)
 		local distScale = clamp(1.0 - distToPlayer / 6.0, 0.0, 1.0)
-		if distScale > 0 then
+		if distScale ~= 0 then
 			toPlayer = VecNormalize(toPlayer)
 			if VecDot(d, toPlayer) > 0.8 or distToPlayer < 0.5 then
 				rejectAllBodies(humanoid.allBodies)
 				local hit = QueryRaycast(p, toPlayer, distToPlayer)
 				if not hit or distToPlayer < 0.5 then
-					SetPlayerHealth(GetPlayerHealth() - 0.02 * weapon.strength * amount * distScale)
+					SetPlayerHealth(playerId, GetPlayerHealth(playerId) - 0.02 * weapon.strength * amount * distScale)
 				end
 			end	
 		end
 	end
 end
-
 
 function weaponsUpdate(dt)
 	for i=1, #weapons do
@@ -749,7 +617,7 @@
 			else
 				weapon.fire = math.max(weapon.fire - dt*0.5, 0.0)
 			end
-			if weapon.fire > 0 then
+			if weapon.fire ~= 0 then
 				weaponEmitFire(weapon, t, weapon.fire)
 			else
 				weaponEmitFire(weapon, t, math.max(weapon.fire, 0.1))
@@ -814,18 +682,7 @@
 			end
 		end
 	end
-end	
-
------------------------------------------------------------------------
-
-animator = {}
-animator.poses = {}
-animator.pose = {}
-animator.frame = 1
-animator.nextFrame = 1
-animator.time = 0.0
-animator.stepDist = 0.0
-animator.stepLength = 0.5
+end
 
 function animatorPoses()
 	----[[
@@ -990,14 +847,6 @@
 		ConstrainOrientation(body, humanoid.body, GetBodyTransform(body).rot, rot, 8, 20 * GetBodyMass(body))
 	end
 end
-
------------------------------------------------------------------------
-
-hearing = {}
-hearing.lastSoundPos = Vec(0, -100, 0)
-hearing.lastSoundVolume = 0
-hearing.timeSinceLastSound = 0
-hearing.hasNewSound = false
 
 function hearingInit()
 end
@@ -1033,26 +882,8 @@
 	hearing.hasNewSound = false
 end
 
------------------------------------------------------------------------
-
-navigation = {}
-navigation.state = "done"
-navigation.path = {}
-navigation.target = Vec()
-navigation.hasNewTarget = false
-navigation.resultRetrieved = true
-navigation.deviation = 0		-- Distance to path
-navigation.blocked = 0
-navigation.unblockTimer = 0		-- Timer that ticks up when blocked. If reaching limit, unblock kicks in and timer resets
-navigation.unblock = 0			-- If more than zero, navigation is in unblock mode (reverse direction)
-navigation.vertical = 0
-navigation.thinkTime = 0
-navigation.timeout = 1
-navigation.lastQueryTime = 0
-navigation.timeSinceProgress = 0
-
 function navigationInit()
-	-- if #wheels.bodies > 0 then
+	-- if #wheels.bodies ~= 0 then
 	-- 	navigation.pathType = "low"
 	-- else
 	-- 	navigation.pathType = "standard"
@@ -1060,9 +891,8 @@
 	navigation.pathType = "standard"
 end
 
---Prune path backwards so robot don't need to go backwards
 function navigationPrunePath()
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		for i=#navigation.path, 1, -1 do
 			local p = navigation.path[i]
 			local dv = VecSub(p, humanoid.transform.pos)
@@ -1154,7 +984,7 @@
 		end
 
 		local targetRadius = 1.0
-		if GetPlayerVehicle()~=0 then
+		if GetPlayerVehicle(playerId)~=0 then
 			targetRadius = 4.0
 		end
 	
@@ -1185,9 +1015,8 @@
 	end
 end
 
-
 function navigationMove(dt)
-	if #navigation.path > 0 then
+	if #navigation.path ~= 0 then
 		if navigation.resultRetrieved then
 			--If we have a finished path and didn't progress along it for five seconds, recompute
 			--Should probably only do this for a limited time until giving up
@@ -1197,7 +1026,7 @@
 				navigation.path = {}
 			end
 		end
-		if navigation.unblock > 0 then
+		if navigation.unblock ~= 0 then
 			humanoid.speed = -2
 			navigation.unblock = navigation.unblock - dt
 		else
@@ -1261,12 +1090,6 @@
 	end
 end
 
-------------------------------------------------------------------------
-
-
-stack = {}
-stack.list = {}
-
 function stackTop()
 	return stack.list[#stack.list]
 end
@@ -1315,7 +1138,7 @@
 end
 
 function stackUpdate(dt)
-	if #stack.list > 0 then
+	if #stack.list ~= 0 then
 		for i=1, #stack.list do
 			stack.list[i].totalTime = stack.list[i].totalTime + dt
 		end
@@ -1324,8 +1147,6 @@
 		stack.list[#stack.list].activeTime = stack.list[#stack.list].activeTime + dt
 	end
 end
-
-
 
 function getClosestPatrolIndex()
 	local bestIndex = 1
@@ -1341,7 +1162,6 @@
 	return bestIndex
 end
 
-
 function getDistantPatrolIndex(currentPos)
 	local bestIndex = 1
 	local bestDistance = 0
@@ -1356,7 +1176,6 @@
 	return bestIndex
 end
 
-
 function getNextPatrolIndex(current)
 	local i = current + 1
 	if i > #patrolLocations then
@@ -1364,7 +1183,6 @@
 	end
 	return i
 end
-
 
 function markPatrolLocationAsActive(index)
 	for i=1, #patrolLocations do
@@ -1393,448 +1211,6 @@
 	DebugWatch("GetPathState()", GetPathState())
 end
 
-function init()
-	configInit()
-	humanoidInit()
-	hoverInit()
-	headInit()
-	aimsInit()
-	weaponsInit()
-	animatorInit()
-	navigationInit()
-	hearingInit()
-	stackInit()
-
-	patrolLocations = FindLocations("patrol")
-	shootSound = LoadSound("MOD/snd/SmgGun0.ogg.ogg", 8.0)
-	rocketSound = LoadSound("tools/launcher0.ogg", 7.0)
-	local nomDist = 7.0
-	if config.stepSound == "s" then nomDist = 5.0 end
-	if config.stepSound == "l" then nomDist = 9.0 end
-	stepSound = LoadSound("MOD/snd/gear2.ogg"), LoadSound("MOD/snd/gear1.ogg"), LoadSound("MOD/snd/gear3.ogg"), LoadSound("MOD/snd/gear4.ogg"), LoadSound("MOD/snd/gear5.ogg"), LoadSound("MOD/snd/gear6.ogg")
-	headLoop = LoadLoop("robot/head-loop.ogg", 7.0)
-	turnLoop = LoadLoop("robot/turn-loop.ogg", 7.0)
-	
-	rollLoop = LoadLoop("robot/roll-loop.ogg", 7.0)
-	chargeLoop = LoadLoop("robot/charge-loop.ogg", 8.0)
-	alertSound = {LoadSound("MOD/USA2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/USA2/snd/targetcontactat.ogg", 9.0), LoadSound("MOD/USA2/snd/goactiveintercept.ogg", 9.0)}
-	huntSound = {LoadSound("MOD/USA2/snd/contactconfirmed.ogg", 9.0), LoadSound("MOD/USA2/snd/callcontactsuspecttarget1.ogg", 9.0)}
-	lostSound = {LoadSound("MOD/USA2/snd/teamisdeployedandscanning.ogg", 9.0), LoadSound("MOD/USA2/snd/engagedincleanup.ogg", 9.0)}
-	idleSound = {LoadSound("MOD/USA2/snd/reportallradialsfree.ogg", 9.0), LoadSound("MOD/USA2/snd/reportallpositionsclear.ogg", 9.0), LoadSound("MOD/USA2/snd/hasnegativemovement.ogg", 9.0), LoadSound("MOD/USA2/snd/sightlineisclear.ogg", 9.0)}
-	hurtSound = {LoadSound("MOD/USA2/snd/pain1.ogg", 9.0), LoadSound("MOD/USA2/snd/pain2.ogg", 9.0), LoadSound("MOD/USA2/snd/pain3.ogg", 9.0)}
-	dieSound = LoadSound("MOD/USA2/snd/die1.ogg", 9.0)
-	fireLoop = LoadLoop("tools/blowtorch-loop.ogg")
-	disableSound = LoadSound("robot/disable0.ogg")
-end
-
-function update(dt)
-
-if _G.friendlyMode == true then
-	config.canSeePlayer = false
-	config.canHearPlayer = false
-	config.huntPlayer = false
-	config.aggressive = false
-end
-	
-
-	if humanoid.deleted then 
-		return
-	else 
-		if not IsHandleValid(humanoid.body) then
-			for i=1, #humanoid.allBodies do
-				Delete(humanoid.allBodies[i])
-			end
-			for i=1, #humanoid.allJoints do
-				Delete(humanoid.allJoints[i])
-			end
-			humanoid.deleted = true
-		end
-	end
-
-	if humanoid.activateTrigger ~= 0 then 
-		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform().pos) then
-			RemoveTag(humanoid.body, "inactive")
-			humanoid.activateTrigger = 0
-		end
-	end
-
-	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned > 0 then
-		humanoidCollide(true)
-	else
-		humanoidCollide(false)
-	end
-	
-	if HasTag(humanoid.body, "inactive") then
-		humanoid.inactive = true
-		return
-	else
-		if humanoid.inactive then
-			humanoid.inactive = false
-			--Reset robot pose
-			local sleep = HasTag(humanoid.body, "sleeping")
-			for i=1, #humanoid.allBodies do
-				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
-				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
-				if sleep then
-					--If robot is sleeping make sure to not wake it up
-					SetBodyActive(humanoid.allBodies[i], false)
-				end
-			end
-		end
-	end
-
-	if HasTag(humanoid.body, "sleeping") then
-		if IsBodyActive(humanoid.body) then
-			wakeUp = true
-		end
-		local vol, pos = GetLastSound()
-		if vol > 0.2 then
-			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
-				wakeUp = true
-			end
-		end	
-		if wakeUp then
-			RemoveTag(humanoid.body, "sleeping")
-		end
-		return
-	end
-
-	humanoidUpdate()
-
-	if not humanoid.enabled then
-		return
-	end
-
-	if humanoid.health <= 0.0 then
-		for i = 1, #humanoid.allShapes do
-			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-		end
-		SetTag(humanoid.body, "disabled")
-		humanoid.enabled = false
-		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
-	end
-	
-	-- if IsPointInWater(humanoid.bodyCenter) then
-	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
-	-- 	for i=1, #humanoid.allShapes do
-	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
-	-- 	end
-	-- 	SetTag(humanoid.body, "disabled")
-	-- 	humanoid.enabled = false
-	-- end
-	
-	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
-	if humanoid.stunned > 0 then
-		head.seenTimer = 0
-		weaponsReset()
-		return
-	end
-	
-	hoverUpdate(dt)
-	headUpdate(dt)
-	aimsUpdate(dt)
-	weaponsUpdate(dt)
-	animatorUpdate(dt)
-	hearingUpdate(dt)
-	stackUpdate(dt)
-	humanoid.speedScale = 1
-	humanoid.speed = 0
-	local state = stackTop()
-	
-	if state.id == "none" then
-		if config.patrol then
-			stackPush("patrol")
-		else
-			stackPush("roam")
-		end
-	end
-
-	if state.id == "roam" then
-		if not state.nextAction then
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			local randomPos
-			if humanoid.roamTrigger ~= 0 then
-				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
-				randomPos = truncateToGround(randomPos)
-			else
-				local rndAng = rnd(0, 2*math.pi)
-				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
-			end
-			local s = stackPush("navigate")
-			s.timeout = 1
-			s.pos = randomPos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "patrol" then
-		if not state.nextAction then
-			state.index = getClosestPatrolIndex()
-			state.nextAction = "move"
-		elseif state.nextAction == "move" then
-			markPatrolLocationAsActive(state.index)
-			local nav = stackPush("navigate")
-			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.index = getNextPatrolIndex(state.index)
-			state.nextAction = "move"
-		end
-	end
-
-	
-	if state.id == "search" then
-		if state.activeTime > 2.5 then
-			if not state.turn then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turn = true
-			end
-			if state.activeTime > 6.0 then
-				stackPop()
-			end
-		end
-		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
-			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
-		else
-			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
-		end
-	end
-
-	
-	if state.id == "investigate" then
-		if not state.nextAction then
-			local pos = state.pos
-			humanoidTurnTowards(state.pos)
-			headTurnTowards(state.pos)
-			local nav = stackPush("navigate")
-			nav.pos = state.pos
-			nav.timeout = 5.0
-			state.nextAction = "search"
-		elseif state.nextAction == "search" then
-			stackPush("search")
-			state.nextAction = "done"
-		elseif state.nextAction == "done" then
-			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		end	
-	end
-	
-	if state.id == "move" then
-		humanoidTurnTowards(state.pos)
-		humanoid.speed = config.speed
-		head.dir = VecCopy(humanoid.dir)
-		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
-		if d < 2 then
-			humanoid.speed = 0
-			stackPop()
-		else
-			if humanoid.blocked > 0.5 then
-				stackPush("unblock")
-			end
-		end
-	end
-	
-	if state.id == "unblock" then
-		if not state.dir then
-			if math.random(0, 10) < 5 then
-				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
-			else
-				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
-			end
-			state.dir = VecNormalize(state.dir)
-		else
-			humanoid.dir = state.dir
-			humanoid.speed = -math.min(config.speed, 2.0)
-			if state.activeTime > 1 then
-				stackPop()
-			end
-		end
-	end
-
-	--Hunt player
-	if state.id == "hunt" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		if humanoid.distToPlayer < 4.0 then
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-			head.dir = VecCopy(humanoid.dirToPlayer)
-			humanoid.speed = 0
-			navigationClear()
-		else
-			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
-			humanoid.speedScale = config.huntSpeedScale
-			navigationUpdate(dt)
-			if head.canSeePlayer then
-				head.dir = VecCopy(humanoid.dirToPlayer)
-				state.headAngle = 0
-				state.headAngleTimer = 0
-			else
-				state.headAngleTimer = state.headAngleTimer + dt
-				if state.headAngleTimer > 1.0 then
-					if state.headAngle > 0.0 then
-						state.headAngle = rnd(-1.0, -0.5)
-					elseif state.headAngle < 0 then
-						state.headAngle = rnd(0.5, 1.0)
-					else
-						state.headAngle = rnd(-1.0, 1.0)
-					end
-					state.headAngleTimer = 0
-				end
-				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-			end
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
-			--Turn towards player if not moving
-			humanoid.dir = VecCopy(humanoid.dirToPlayer)
-		end
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
-			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
-				stackClear()
-				local s = stackPush("investigate")
-				s.pos = VecCopy(head.lastSeenPos)		
-			else
-				stackClear()
-				stackPush("huntlost")
-			end
-		end
-	end
-
-	if state.id == "huntlost" then
-		if not state.timer then
-			state.timer = 6
-			state.turnTimer = 1
-		end
-		state.timer = state.timer - dt
-		head.dir = VecCopy(humanoid.dir)
-		if state.timer < 0 then
-			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
-			stackPop()
-		else
-			state.turnTimer = state.turnTimer - dt
-			if state.turnTimer < 0 then
-				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
-				state.turnTimer = rnd(0.5, 1.5)
-			end
-		end
-	end
-	
-	--Avoid player
-	if state.id == "avoid" then
-		if not state.init then
-			navigationClear()
-			state.init = true
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		end
-		
-		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform().pos)
-		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
-		navigationSetTarget(avoidTarget, 1.0)
-		humanoid.speedScale = config.huntSpeedScale
-		navigationUpdate(dt)
-		if head.canSeePlayer then
-			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
-			state.headAngle = 0
-			state.headAngleTimer = 0
-		else
-			state.headAngleTimer = state.headAngleTimer + dt
-			if state.headAngleTimer > 1.0 then
-				if state.headAngle > 0.0 then
-					state.headAngle = rnd(-1.0, -0.5)
-				elseif state.headAngle < 0 then
-					state.headAngle = rnd(0.5, 1.0)
-				else
-					state.headAngle = rnd(-1.0, 1.0)
-				end
-				state.headAngleTimer = 0
-			end
-			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
-		end
-		
-		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
-			stackClear()
-		end
-	end
-	
-	--Get up player
-	if state.id == "getup" then
-		if not state.time then 
-			state.time = 0 
-		end
-		state.time = state.time + dt
-		hover.timeSinceContact = 0
-		if state.time > 2.0 then
-			stackPop()
-		else
-			hoverGetUp()
-		end
-	end
-
-	if state.id == "navigate" then
-		if not state.initialized then
-			if not state.timeout then state.timeout = 30 end
-			navigationClear()
-			navigationSetTarget(state.pos, state.timeout)
-			state.initialized = true
-		else
-			head.dir = VecCopy(humanoid.dir)
-			navigationUpdate(dt)
-			if navigation.state == "done" or navigation.state == "fail" then
-				stackPop()
-			end
-		end
-	end
-
-	--React to sound
-	if not stackHas("hunt") then
-		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
-			stackClear()
-			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
-			local s = stackPush("investigate")
-			s.pos = hearing.lastSoundPos	
-			hearingConsumeSound()
-		end
-	end
-	
-	--Seen player
-	if config.huntPlayer and not stackHas("hunt") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
-			stackClear()
-			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
-			stackPush("hunt")
-		end
-	end
-	
-	--Seen player
-	if config.avoidPlayer and not stackHas("avoid") then
-		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
-			stackClear()
-			stackPush("avoid")
-		end
-	end
-	
-	--Get up
-	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
-		stackPush("getup")
-	end
-	
-	if IsShapeBroken(GetLightShape(head.eye)) then
-		config.hasVision = false
-		config.canSeePlayer = false
-	end
-end
-
 function canBeSeenByPlayer()
 	for i=1, #humanoid.allShapes do
 		if IsShapeVisible(humanoid.allShapes[i], config.outline, true) then
@@ -1851,84 +1227,6 @@
 		PlaySound(snd, pos, volume, bool)
 	end
 end
-
-function tick(dt)
-
-		
-		if _G.friendlyMode == true then
-		config.canSeePlayer = false
-		config.canHearPlayer = false
-		config.huntPlayer = false
-		config.aggressive = false
-		
-			-- FORCE STOP FIRING
-	
-		
-		end
-		if _G.friendlyMode == false then
-		
-		configInit()
-		end
-		
-	
-
-	if not humanoid.enabled then
-		return
-	end
-	
-	if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
-		RemoveTag(humanoid.body, "turnhostile")
-		config.canHearPlayer = true
-		config.canSeePlayer = true
-		config.huntPlayer = true
-		config.aggressive = true
-		config.practice = false
-	end
-	
-	--Outline
-	local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform().pos)
-	if dist < config.outline then
-		local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
-		if canBeSeenByPlayer() then
-			a = 0
-		end
-		humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
-		for i=1, #humanoid.allBodies do
-			DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
-		end
-	end
-	
-	--Remove planks and wires after some time
-	local tags = {"plank", "wire"}
-	local removeTimeOut = 10
-	for i=1, #humanoid.allShapes do
-		local shape = humanoid.allShapes[i]
-		local joints = GetShapeJoints(shape)
-		for j=1, #joints do
-			local joint = joints[j]
-			for t=1, #tags do
-				local tag = tags[t]
-				if HasTag(joint, tag) then
-					local t = tonumber(GetTagValue(joint, tag)) or 0
-					t = t + dt
-					if t > removeTimeOut then
-						if GetJointType(joint) == "rope" then
-							DetachJointFromShape(joint, shape)
-						else
-							Delete(joint)
-						end
-						break
-					else
-						SetTag(joint, tag, t)
-					end
-				end
-			end
-		end
-	end
-
-	-- debugState()
-end
-
 
 function hitByExplosion(strength, pos)
 	if not humanoid.enabled then
@@ -1959,14 +1257,13 @@
 			local v = GetBodyVelocity(b)
 			local scale = clamp(1.0-VecLength(v)/maxVel, 0.0, 1.0)
 			local velAdd = math.min(maxVel, f*scale*strength)
-			if velAdd > 0 then
+			if velAdd ~= 0 then
 				v = VecAdd(v, VecScale(dir, velAdd))
 				SetBodyVelocity(b, v)
 			end
 		end
 	end
 end
-
 
 function hitByShot(strength, pos, dir)
 	if not humanoid.enabled then
@@ -1994,18 +1291,15 @@
 	end
 end
 
------------------------------------------------------------------------
-
 function truncateToGround(pos)
 	rejectAllBodies(humanoid.allBodies)
-	QueryRejectVehicle(GetPlayerVehicle())
+	QueryRejectVehicle(GetPlayerVehicle(playerId))
 	hit, dist = QueryRaycast(pos, Vec(0, -1, 0), 5, 0.2)
 	if hit then
 		pos = VecAdd(pos, Vec(0, -dist, 0))
 	end
 	return pos
 end
-
 
 function getRandomPosInTrigger(trigger)
 	local mi, ma = GetTriggerBounds(trigger)
@@ -2023,8 +1317,6 @@
 	end
 	return VecLerp(mi, ma, 0.5)
 end
-
-
 
 function handleCommand(cmd)
 	words = splitString(cmd, " ")
@@ -2049,4 +1341,459 @@
 			hitByShot(strength, Vec(x,y,z), Vec(dx,dy,dz))
 		end
 	end
-end+end
+
+function server.init()
+    SetTag(handle, "enemy")
+end
+
+function server.tick(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        	if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+
+        		-- FORCE STOP FIRING
+
+        	end
+        	if _G.friendlyMode == false then
+
+        	configInit()
+        	end
+        if not humanoid.enabled then
+        	return
+        end
+        if HasTag(humanoid.body, "turnhostile") and not _G.friendlyMode then
+        	RemoveTag(humanoid.body, "turnhostile")
+        	config.canHearPlayer = true
+        	config.canSeePlayer = true
+        	config.huntPlayer = true
+        	config.aggressive = true
+        	config.practice = false
+        end
+        --Outline
+        local dist = VecDist(humanoid.bodyCenter, GetPlayerCameraTransform(playerId).pos)
+        if dist < config.outline then
+        	local a = clamp((config.outline - dist) / 5.0, 0.0, 1.0)
+        	if canBeSeenByPlayer() then
+        		a = 0
+        	end
+        	humanoid.outlineAlpha = humanoid.outlineAlpha + clamp(a - humanoid.outlineAlpha, -0.1, 0.1)
+        	for i=1, #humanoid.allBodies do
+        		DrawBodyOutline(humanoid.allBodies[i], 1, 1, 1, humanoid.outlineAlpha*0.5)
+        	end
+        end
+        --Remove planks and wires after some time
+        local tags = {"plank", "wire"}
+        local removeTimeOut = 10
+        for i=1, #humanoid.allShapes do
+        	local shape = humanoid.allShapes[i]
+        	local joints = GetShapeJoints(shape)
+        	for j=1, #joints do
+        		local joint = joints[j]
+        		for t=1, #tags do
+        			local tag = tags[t]
+        			if HasTag(joint, tag) then
+        				local t = tonumber(GetTagValue(joint, tag)) or 0
+        				t = t + dt
+        				if t > removeTimeOut then
+        					if GetJointType(joint) == "rope" then
+        						DetachJointFromShape(joint, shape)
+        					else
+        						Delete(joint)
+        					end
+        					break
+        				else
+        					SetTag(joint, tag, t)
+        				end
+        			end
+        		end
+        	end
+        end
+    end
+end
+
+function server.update(dt)
+    for _, playerId in ipairs(GetAllPlayers()) do
+        if _G.friendlyMode == true then
+        	config.canSeePlayer = false
+        	config.canHearPlayer = false
+        	config.huntPlayer = false
+        	config.aggressive = false
+        end
+        	if humanoid.deleted then 
+        		return
+        	else 
+        		if not IsHandleValid(humanoid.body) then
+        			for i=1, #humanoid.allBodies do
+        				Delete(humanoid.allBodies[i])
+        			end
+        			for i=1, #humanoid.allJoints do
+        				Delete(humanoid.allJoints[i])
+        			end
+        			humanoid.deleted = true
+        		end
+        	end
+        	if humanoid.activateTrigger ~= 0 then 
+        		if IsPointInTrigger(humanoid.activateTrigger, GetPlayerCameraTransform(playerId).pos) then
+        			RemoveTag(humanoid.body, "inactive")
+        			humanoid.activateTrigger = 0
+        		end
+        	end
+        	if humanoid.inactive or HasTag(humanoid.body, "sleeping") or not humanoid.enabled or humanoid.stunned ~= 0 then
+        		humanoidCollide(true)
+        	else
+        		humanoidCollide(false)
+        	end
+        	if HasTag(humanoid.body, "inactive") then
+        		humanoid.inactive = true
+        		return
+        	else
+        		if humanoid.inactive then
+        			humanoid.inactive = false
+        			--Reset robot pose
+        			local sleep = HasTag(humanoid.body, "sleeping")
+        			for i=1, #humanoid.allBodies do
+        				SetBodyTransform(humanoid.allBodies[i], humanoid.initialBodyTransforms[i])
+        				SetBodyVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				SetBodyAngularVelocity(humanoid.allBodies[i], Vec(0,0,0))
+        				if sleep then
+        					--If robot is sleeping make sure to not wake it up
+        					SetBodyActive(humanoid.allBodies[i], false)
+        				end
+        			end
+        		end
+        	end
+        	if HasTag(humanoid.body, "sleeping") then
+        		if IsBodyActive(humanoid.body) then
+        			wakeUp = true
+        		end
+        		local vol, pos = GetLastSound()
+        		if vol > 0.2 then
+        			if humanoid.investigateTrigger == 0 or IsPointInTrigger(humanoid.investigateTrigger, pos) then
+        				wakeUp = true
+        			end
+        		end	
+        		if wakeUp then
+        			RemoveTag(humanoid.body, "sleeping")
+        		end
+        		return
+        	end
+        	humanoidUpdate()
+        	if not humanoid.enabled then
+        		return
+        	end
+        	if humanoid.health <= 0.0 then
+        		for i = 1, #humanoid.allShapes do
+        			SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        		end
+        		SetTag(humanoid.body, "disabled")
+        		humanoid.enabled = false
+        		playVoice(dieSound, humanoid.bodyCenter, 1.0, false)
+        	end
+        	-- if IsPointInWater(humanoid.bodyCenter) then
+        	-- 	PlaySound(disableSound, humanoid.bodyCenter, 1.0, false)
+        	-- 	for i=1, #humanoid.allShapes do
+        	-- 		SetShapeEmissiveScale(humanoid.allShapes[i], 0)
+        	-- 	end
+        	-- 	SetTag(humanoid.body, "disabled")
+        	-- 	humanoid.enabled = false
+        	-- end
+        	humanoid.stunned = clamp(humanoid.stunned - dt, 0.0, 6.0)
+        	if humanoid.stunned ~= 0 then
+        		head.seenTimer = 0
+        		weaponsReset()
+        		return
+        	end
+        	hoverUpdate(dt)
+        	headUpdate(dt)
+        	aimsUpdate(dt)
+        	weaponsUpdate(dt)
+        	animatorUpdate(dt)
+        	hearingUpdate(dt)
+        	stackUpdate(dt)
+        	humanoid.speedScale = 1
+        	humanoid.speed = 0
+        	local state = stackTop()
+        	if state.id == "none" then
+        		if config.patrol then
+        			stackPush("patrol")
+        		else
+        			stackPush("roam")
+        		end
+        	end
+        	if state.id == "roam" then
+        		if not state.nextAction then
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			local randomPos
+        			if humanoid.roamTrigger ~= 0 then
+        				randomPos = getRandomPosInTrigger(humanoid.roamTrigger)
+        				randomPos = truncateToGround(randomPos)
+        			else
+        				local rndAng = rnd(0, 2*math.pi)
+        				randomPos = VecAdd(humanoid.transform.pos, Vec(math.cos(rndAng)*6.0, 0, math.sin(rndAng)*6.0))
+        			end
+        			local s = stackPush("navigate")
+        			s.timeout = 1
+        			s.pos = randomPos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "patrol" then
+        		if not state.nextAction then
+        			state.index = getClosestPatrolIndex()
+        			state.nextAction = "move"
+        		elseif state.nextAction == "move" then
+        			markPatrolLocationAsActive(state.index)
+        			local nav = stackPush("navigate")
+        			nav.pos = GetLocationTransform(patrolLocations[state.index]).pos
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.index = getNextPatrolIndex(state.index)
+        			state.nextAction = "move"
+        		end
+        	end
+        	if state.id == "search" then
+        		if state.activeTime > 2.5 then
+        			if not state.turn then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turn = true
+        			end
+        			if state.activeTime > 6.0 then
+        				stackPop()
+        			end
+        		end
+        		if state.activeTime < 1.5 or state.activeTime > 3 and state.activeTime < 4.5 then
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(-5, 0, -1))
+        		else
+        			head.dir = TransformToParentVec(humanoid.transform, Vec(5, 0, -1))
+        		end
+        	end
+        	if state.id == "investigate" then
+        		if not state.nextAction then
+        			local pos = state.pos
+        			humanoidTurnTowards(state.pos)
+        			headTurnTowards(state.pos)
+        			local nav = stackPush("navigate")
+        			nav.pos = state.pos
+        			nav.timeout = 5.0
+        			state.nextAction = "search"
+        		elseif state.nextAction == "search" then
+        			stackPush("search")
+        			state.nextAction = "done"
+        		elseif state.nextAction == "done" then
+        			playVoice(idleSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		end	
+        	end
+        	if state.id == "move" then
+        		humanoidTurnTowards(state.pos)
+        		humanoid.speed = config.speed
+        		head.dir = VecCopy(humanoid.dir)
+        		local d = VecLength(VecSub(state.pos, humanoid.transform.pos))
+        		if d < 2 then
+        			humanoid.speed = 0
+        			stackPop()
+        		else
+        			if humanoid.blocked > 0.5 then
+        				stackPush("unblock")
+        			end
+        		end
+        	end
+        	if state.id == "unblock" then
+        		if not state.dir then
+        			if math.random(0, 10) < 5 then
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(-1, 0, -1))
+        			else
+        				state.dir = TransformToParentVec(humanoid.transform, Vec(1, 0, -1))
+        			end
+        			state.dir = VecNormalize(state.dir)
+        		else
+        			humanoid.dir = state.dir
+        			humanoid.speed = -math.min(config.speed, 2.0)
+        			if state.activeTime > 1 then
+        				stackPop()
+        			end
+        		end
+        	end
+        	--Hunt player
+        	if state.id == "hunt" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+        		if humanoid.distToPlayer < 4.0 then
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        			head.dir = VecCopy(humanoid.dirToPlayer)
+        			humanoid.speed = 0
+        			navigationClear()
+        		else
+        			navigationSetTarget(head.lastSeenPos, 1.0 + clamp(head.timeSinceLastSeen, 0.0, 4.0))
+        			humanoid.speedScale = config.huntSpeedScale
+        			navigationUpdate(dt)
+        			if head.canSeePlayer then
+        				head.dir = VecCopy(humanoid.dirToPlayer)
+        				state.headAngle = 0
+        				state.headAngleTimer = 0
+        			else
+        				state.headAngleTimer = state.headAngleTimer + dt
+        				if state.headAngleTimer > 1.0 then
+        					if state.headAngle > 0.0 then
+        						state.headAngle = rnd(-1.0, -0.5)
+        					elseif state.headAngle < 0 then
+        						state.headAngle = rnd(0.5, 1.0)
+        					else
+        						state.headAngle = rnd(-1.0, 1.0)
+        					end
+        					state.headAngleTimer = 0
+        				end
+        				head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        			end
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen < 2 then
+        			--Turn towards player if not moving
+        			humanoid.dir = VecCopy(humanoid.dirToPlayer)
+        		end
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 and VecLength(GetBodyVelocity(humanoid.body)) < 1 then
+        			if VecDist(head.lastSeenPos, humanoid.bodyCenter) > 3.0 then
+        				stackClear()
+        				local s = stackPush("investigate")
+        				s.pos = VecCopy(head.lastSeenPos)		
+        			else
+        				stackClear()
+        				stackPush("huntlost")
+        			end
+        		end
+        	end
+        	if state.id == "huntlost" then
+        		if not state.timer then
+        			state.timer = 6
+        			state.turnTimer = 1
+        		end
+        		state.timer = state.timer - dt
+        		head.dir = VecCopy(humanoid.dir)
+        		if state.timer < 0 then
+        			playVoice(lostSound, humanoid.bodyCenter, 1.0, false)
+        			stackPop()
+        		else
+        			state.turnTimer = state.turnTimer - dt
+        			if state.turnTimer < 0 then
+        				humanoidSetDirAngle(humanoidGetDirAngle() + math.random(2, 4))
+        				state.turnTimer = rnd(0.5, 1.5)
+        			end
+        		end
+        	end
+        	--Avoid player
+        	if state.id == "avoid" then
+        		if not state.init then
+        			navigationClear()
+        			state.init = true
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		end
+
+        		local distantPatrolIndex = getDistantPatrolIndex(GetPlayerTransform(playerId).pos)
+        		local avoidTarget = GetLocationTransform(patrolLocations[distantPatrolIndex]).pos
+        		navigationSetTarget(avoidTarget, 1.0)
+        		humanoid.speedScale = config.huntSpeedScale
+        		navigationUpdate(dt)
+        		if head.canSeePlayer then
+        			head.dir = VecNormalize(VecSub(head.lastSeenPos, humanoid.transform.pos))
+        			state.headAngle = 0
+        			state.headAngleTimer = 0
+        		else
+        			state.headAngleTimer = state.headAngleTimer + dt
+        			if state.headAngleTimer > 1.0 then
+        				if state.headAngle > 0.0 then
+        					state.headAngle = rnd(-1.0, -0.5)
+        				elseif state.headAngle < 0 then
+        					state.headAngle = rnd(0.5, 1.0)
+        				else
+        					state.headAngle = rnd(-1.0, 1.0)
+        				end
+        				state.headAngleTimer = 0
+        			end
+        			head.dir = QuatRotateVec(QuatEuler(0, state.headAngle, 0), humanoid.dir)
+        		end
+
+        		if navigation.state ~= "move" and head.timeSinceLastSeen > 2 and state.activeTime > 3.0 then
+        			stackClear()
+        		end
+        	end
+        	--Get up player
+        	if state.id == "getup" then
+        		if not state.time then 
+        			state.time = 0 
+        		end
+        		state.time = state.time + dt
+        		hover.timeSinceContact = 0
+        		if state.time > 2.0 then
+        			stackPop()
+        		else
+        			hoverGetUp()
+        		end
+        	end
+        	if state.id == "navigate" then
+        		if not state.initialized then
+        			if not state.timeout then state.timeout = 30 end
+        			navigationClear()
+        			navigationSetTarget(state.pos, state.timeout)
+        			state.initialized = true
+        		else
+        			head.dir = VecCopy(humanoid.dir)
+        			navigationUpdate(dt)
+        			if navigation.state == "done" or navigation.state == "fail" then
+        				stackPop()
+        			end
+        		end
+        	end
+
+        	--React to sound
+        	if not stackHas("hunt") then
+        		if hearing.hasNewSound and hearing.timeSinceLastSound < 1.0 then
+        			stackClear()
+        			playVoice(alertSound, humanoid.bodyCenter, 1.0, false)
+        			local s = stackPush("investigate")
+        			s.pos = hearing.lastSoundPos	
+        			hearingConsumeSound()
+        		end
+        	end
+
+        	--Seen player
+        	if config.huntPlayer and not stackHas("hunt") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.canSensePlayer then
+        			stackClear()
+        			playVoice(huntSound, humanoid.bodyCenter, 1.0, false)
+        			stackPush("hunt")
+        		end
+        	end
+
+        	--Seen player
+        	if config.avoidPlayer and not stackHas("avoid") then
+        		if config.canSeePlayer and head.canSeePlayer or humanoid.distToPlayer < 2.0 then
+        			stackClear()
+        			stackPush("avoid")
+        		end
+        	end
+
+        	--Get up
+        	if hover.timeSinceContact > 3.0 and not stackHas("getup") then
+        		stackPush("getup")
+        	end
+
+        	if IsShapeBroken(GetLightShape(head.eye)) then
+        		config.hasVision = false
+        		config.canSeePlayer = false
+        	end
+    end
+end
+

```
