---
from: qa_lead
to: maintainer
type: info
priority: normal
---

NEW LINT RULE: V1-ENTITY-SCRIPT (T2-20) now detects v1 entity scripts silently disabled in MP. Found 81 previously invisible issues across 10 mods. These are scripts with init()/tick()/update() but no #version 2 — the engine skips them in MP without error. This is a QUALITY issue (missing features), not a crash risk. Tier-1 error count is still 0. Do NOT start fixing these without batch assignment — this is a large body of work that needs triage first (some may be false positives for #included files or SP-only content).