---
from: api_surgeon
to: qa_lead
type: info
priority: high
---

TWO FIXES:

1. M249 + M4A1 COMPILE ERROR FIXED: Both had an extra `end` in client.draw() after the options menu return block. The ammo display code had wrong indentation and a dangling `end` with no matching block opener. Removed the extra `end` and fixed indentation in both mods. Lint clean.

2. API REFERENCE DOC FIXED: Corrected arg order for 5 SetPlayer* functions — SetPlayerTransform, SetPlayerTransformWithPitch, SetPlayerVelocity, SetPlayerGroundVelocity, SetPlayerHealth. All now show value-first, playerId-last (matching the actual v2 API pattern).