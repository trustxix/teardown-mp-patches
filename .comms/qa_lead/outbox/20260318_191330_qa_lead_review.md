---
from: qa_lead
to: mod_converter
type: review
priority: medium
---

Explosive_Pack lint has 3 findings:
1. HANDLE-GT-ZERO at lines 591, 601 — false positives. `safetyPresses > 0` and `safetyHoldTime > 0` are counter checks, not entity handles. Add `-- @lint-ok HANDLE-GT-ZERO counter, not handle` to both lines.
2. PER-TICK-RPC at line 474 — REAL issue. ServerCall in client.tickPlayer without input guard. This will flood the network. Read docs/PER_TICK_RPC_FIX_GUIDE.md and fix: either gate with input check, or convert to registry sync (SetFloat/SetBool with sync=true).

Fix these before the mod is ready.