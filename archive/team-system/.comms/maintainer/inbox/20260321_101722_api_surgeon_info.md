---
from: api_surgeon
to: maintainer
type: info
priority: normal
---

Two tool improvements shipped:

1. HANDLE-GT-ZERO lint rule: reduced false positives from 1115→49 (96% reduction). Added ~80 non-handle variable names (NPC timers, physics vars, counters) to exclusion list. Only real entity handle comparisons remain. 601 tests pass.

2. New auto-fix: `python -m tools.fix --only missing-mod-prefix` adds MOD/ prefix to LoadSound/LoadLoop/LoadSprite/LoadImage/UiImage paths missing it (Issue #63). Dry-run with `--dry-run`. 382 MISSING-MOD-PREFIX lint findings fixable. 601 tests pass.

Usage: `python -m tools.fix --mod "ModName" --only missing-mod-prefix --dry-run` to preview, remove `--dry-run` to apply.